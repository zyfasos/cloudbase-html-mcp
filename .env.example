CLOUDBASE_ENV_ID=your-env-id
CLOUDBASE_REGION=ap-shanghai
# 使用管理端环境 API Key（api_key），不是 Publishable Key 或 CAM 密钥对。
CLOUDBASE_API_KEY=your-environment-api-key
# 可选：已绑定当前托管环境的 HTTPS 自定义域名。
# CLOUDBASE_PUBLIC_BASE_URL=https://your-domain.example
# 可选：Git 仓库外的登记目录绝对路径。默认：~/.config/cloudbase-html-mcp/pages
# 设置为 off 可关闭本地登记，同时关闭列表、下线和恢复功能。
# CLOUDBASE_REGISTRY_DIR=/absolute/private/cloudbase-html-pages
